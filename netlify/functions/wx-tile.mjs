function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}})}
export default async(req)=>{try{const u=new URL(req.url),lat=Number(u.searchParams.get('lat')),lon=Number(u.searchParams.get('lon')),z=7;if(!Number.isFinite(lat)||!Number.isFinite(lon))return json({error:'invalid lat/lon'},400);const mr=await fetch('https://api.rainviewer.com/public/weather-maps.json'),m=await mr.json(),f=(m?.radar?.past||[]).at(-1);if(!f)throw new Error('No RainViewer radar frames');const url=`${m.host}${f.path}/512/${z}/${lat.toFixed(5)}/${lon.toFixed(5)}/2/1_0.png`;const r=await fetch(url);if(!r.ok)throw new Error(`RainViewer HTTP ${r.status}`);return new Response(await r.arrayBuffer(),{status:200,headers:{'content-type':'image/png','cache-control':'no-store','x-wx-time':String(f.time||'')}})}catch(e){return json({error:String(e)},502)}};
export const config={path:'/api/wx/tile'};
